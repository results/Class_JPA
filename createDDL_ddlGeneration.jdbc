CREATE TABLE employees (employeeNumber NUMBER(10) NOT NULL, EMAIL VARCHAR, EXTENSION VARCHAR, FIRSTNAME VARCHAR, JOBTITLE VARCHAR, LASTNAME VARCHAR, OFFICECODE NUMBER(10), PRIMARY KEY (employeeNumber))
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMBER(19), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN_IDENTITY', 0)
